plugins {
    id 'org.jetbrains.kotlin.multiplatform' version '1.4.10'
}

repositories {
    mavenCentral()
}

kotlin {
  linuxX64("native") {
    binaries {
      executable()
    }
  }
}

wrapper {
  gradleVersion = "6.7"
  distributionType = "ALL"
}
